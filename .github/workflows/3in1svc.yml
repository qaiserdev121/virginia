
name: 3 in 1 

#on:
#  push:
#    branches:
#      - mai
on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  terraformjob:
    runs-on: ubuntu-latest

    env:
      ARM_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
      ARM_USE_OIDC: "true"

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
# user pipeline
      - name: Terraform Init
        run: terraform init
        working-directory: "svcacUser"

      - name: Terraform Plan
        run: terraform plan
        working-directory: "svcacUser"

      - name: Terraform Apply
        run: terraform apply -auto-approve
        working-directory: "svcacUser"

# container pipeline        
      - name: Terraform Init
        run: terraform init
        working-directory: "svccontainermodule"

      - name: Terraform Plan
        run: terraform plan
        working-directory: "svccontainermodule"

      - name: Terraform Apply
        run: terraform apply -auto-approve
        working-directory: "svccontainermodule
# role access        
      - name: Terraform Init
        run: terraform init
        working-directory: "svcreadmodule"

      - name: Terraform Plan
        run: terraform plan
        working-directory: "svcreadmodule"

      - name: Terraform Apply
        run: terraform apply -auto-approve
        working-directory: "svcreadmodule"        