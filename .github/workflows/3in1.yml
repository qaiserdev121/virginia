
name: 3 in 1 

#on:
#  push:
#    branches:
#      - mai
on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  terraformjob:
    runs-on: ubuntu-latest

    env:
      ARM_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
      ARM_USE_OIDC: "true"

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
# user pipeline
      - name: Terraform Init
        run: terraform init
        working-directory: "singleuser"

      - name: Terraform Plan
        run: terraform plan
        working-directory: "singleuser"

      - name: Terraform Apply
        run: terraform apply -auto-approve
        working-directory: "singleuser"

# container pipeline        
      - name: Terraform Init
        run: terraform init
        working-directory: "containermodules"

      - name: Terraform Plan
        run: terraform plan
        working-directory: "containermodules"

      - name: Terraform Apply
        run: terraform apply -auto-approve
        working-directory: "containermodules"
# role access        
      - name: Terraform Init
        run: terraform init
        working-directory: "readmodule"

      - name: Terraform Plan
        run: terraform plan
        working-directory: "readmodule"

      - name: Terraform Apply
        run: terraform apply -auto-approve
        working-directory: "readmodule"        